import React from 'react';
import { View, Text} from 'react-native';

export default function ChatScreen(){
    return(
        <View>
            <Text>This is the Chatscreen im testing the API on don't touch!</Text>
        </View>
    )

}